type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
}

type Company @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  logo: String
}
 